## What changed

## Why

## Testing

- [ ] `swift build` and `swift test` pass
- [ ] `swiftlint lint --quiet` and `swift format lint --recursive Sources Tests Package.swift` pass
- [ ] Verified manually via `make app && make run` (for UI-visible changes)
- [ ] Added a line under `## [Unreleased]` in `CHANGELOG.md` (if user-visible)
